# Quick start

<Subtitle>A quick guide to getting started with Base UI for Lit.</Subtitle>
<Meta name="description" content="A quick guide to getting started with Base UI for Lit." />

## Install the library

Install Lit and the Base UI Lit package with your package manager of choice.

```bash title="Terminal"
npm i lit @base-ui/lit
```

Base UI for Lit exposes tree-shakable component entrypoints, so you can import only the primitives you use.

## Set up

### Create an application root

Lit renders templates into a DOM container. Start with a dedicated mount node for your app.

```html title="index.html"
<div id="app"></div>
```

### Render a component

Base UI for Lit exposes custom elements that you render with Lit's `html` tagged templates and `render()` API.

```ts title="main.ts"
import { html, render } from 'lit';
import '@base-ui/lit/button';

const app = document.querySelector('#app');

if (app == null) {
  throw new Error('Missing #app container');
}

render(html`<button-root>Submit</button-root>`, app);
```

### Styling

Like Base UI for React, the Lit package is unstyled. The examples below use CSS Modules and Tailwind, but you can use plain CSS or any styling solution that fits your stack.

## Assemble a component

This demo shows how to compose a small Lit form card from multiple primitives and apply styles. It includes a label, input, validation message, and action button.

import { DemoQuickStartHero } from './demos/hero';

<DemoQuickStartHero />

### Compose primitives

Complex controls are assembled from nested custom elements in the same render tree. For example, `Field` is composed from `field-root`, `field-label`, `field-control`, `field-error`, and `field-description`:

```ts title="field.ts"
import { html, render } from 'lit';
import '@base-ui/lit/field';

render(
  html`<field-root>
    <field-label>Name</field-label>
    <field-control required></field-control>
    <field-error match="valueMissing">Required</field-error>
    <field-description>Visible on your profile</field-description>
  </field-root>`,
  app,
);
```

## Next steps

This walkthrough covers the basics of installing Base UI for Lit, rendering a primitive, and composing a more complete control.
Start with the [Button docs](/lit/components/button), [Field docs](/lit/components/field), and [Checkbox docs](/lit/components/checkbox) to explore the current Lit surface area.

## Working with LLMs

Each docs page includes a "View as Markdown" link. The docs build also generates Lit markdown exports and includes them in `llms.txt` and `llms-full.txt`.

export const metadata = {
  keywords: [
    'Base UI Lit Quick Start',
    'Lit component library',
    'Lit quick start',
    'Base UI Lit installation',
    'Lit render API',
    'Lit field example',
    'Lit form field',
  ],
};
