# Localization Provider

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/utils/localization-provider"'

## API Reference

### getLocalizationContext

Returns the localization context from the nearest provider.

**Parameters:**

| Parameter | Type      | Default | Description |
| :-------- | :-------- | :------ | :---------- |
| element   | `Element` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = LocalizationContext;
```

### LocalizationProvider

Defines the temporal locale provider for Base UI temporal components.

**LocalizationProvider Props:**

| Prop           | Type     | Default | Description                               |
| :------------- | :------- | :------ | :---------------------------------------- |
| temporalLocale | `Locale` | `en-US` | The locale to use in temporal components. |

### LocalizationProvider.Props

Re-export of [LocalizationProvider](#localizationprovider) props.

## Additional Types

### LocalizationContext

```typescript
type LocalizationContext = {
  /** The locale to use in temporal components. */
  temporalLocale?: Locale;
};
```

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `LocalizationProvider.Props`: `LocalizationProviderProps`
