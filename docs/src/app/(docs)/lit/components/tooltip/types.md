# Tooltip

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/components/tooltip"'

## API Reference

### Root

Groups all parts of the tooltip.
Renders a `<tooltip-root>` custom element.

**Root Props:**

| Prop                  | Type                                                          | Default | Description |
| :-------------------- | :------------------------------------------------------------ | :------ | :---------- |
| defaultOpen           | `boolean`                                                     | -       | -           |
| open                  | `boolean`                                                     | -       | -           |
| onOpenChange          | `((open: boolean, details: Root.ChangeEventDetails) => void)` | -       | -           |
| actionsRef            | `{ current: Root.Actions \| null }`                           | -       | -           |
| defaultTriggerId      | `string \| null`                                              | -       | -           |
| handle                | `Handle`                                                      | -       | -           |
| onOpenChangeComplete  | `((open: boolean) => void)`                                   | -       | -           |
| triggerId             | `string \| null`                                              | -       | -           |
| disabled              | `boolean`                                                     | -       | -           |
| disableHoverablePopup | `boolean`                                                     | -       | -           |
| children              | `unknown`                                                     | -       | -           |

### Root.Props

Re-export of [Root](#root) props.

### Root.Actions

```typescript
type RootActions = { close: close; unmount: unmount };
```

### Root.ChangeEventReason

```typescript
type RootChangeEventReason =
  | 'trigger-hover'
  | 'trigger-focus'
  | 'trigger-press'
  | 'disabled'
  | 'imperative-action'
  | 'escape-key'
  | 'none';
```

### Root.ChangeEventDetails

```typescript
type RootChangeEventDetails = {
  reason: Root.ChangeEventReason;
  event: Event;
  trigger: Element | undefined;
  cancel: cancel;
  allowPropagation: allowPropagation;
  isCanceled: boolean;
  isPropagationAllowed: boolean;
};
```

### Provider

Provides a shared delay for multiple tooltips.
Renders a `<tooltip-provider>` custom element.

**Provider Props:**

| Prop       | Type             | Default | Description |
| :--------- | :--------------- | :------ | :---------- |
| delay      | `number \| null` | -       | -           |
| closeDelay | `number \| null` | -       | -           |
| timeout    | `number`         | -       | -           |
| children   | `unknown`        | -       | -           |

### Provider.Props

Re-export of [Provider](#provider) props.

### Trigger

An element to attach the tooltip to.
Renders a `<tooltip-trigger>` custom element.

**Trigger Props:**

| Prop         | Type             | Default | Description |
| :----------- | :--------------- | :------ | :---------- |
| closeOnClick | `boolean`        | -       | -           |
| handle       | `Handle`         | -       | -           |
| payload      | `unknown`        | -       | -           |
| disabled     | `boolean`        | -       | -           |
| delay        | `number \| null` | -       | -           |
| closeDelay   | `number \| null` | -       | -           |
| id           | `string`         | -       | -           |
| children     | `unknown`        | -       | -           |

**Trigger Data Attributes:**

| Attribute             | Type | Description |
| :-------------------- | :--- | :---------- |
| data-popup-open       | -    | -           |
| data-trigger-disabled | -    | -           |

### Trigger.Props

Re-export of [Trigger](#trigger) props.

### Trigger.State

```typescript
type TriggerState = { open: boolean; disabled: boolean };
```

### Trigger.DataAttributes

Re-export of [Trigger](#trigger) data-attributes.

### Portal

A portal element that moves the popup to a different part of the DOM.
By default, the portal element is appended to `<body>`.
Renders a `<tooltip-portal>` custom element.

**Portal Props:**

| Prop      | Type                                | Default | Description |
| :-------- | :---------------------------------- | :------ | :---------- |
| container | `HTMLElement \| ShadowRoot \| null` | -       | -           |
| children  | `unknown`                           | -       | -           |

### Portal.Props

Re-export of [Portal](#portal) props.

### Positioner

Positions the tooltip against the trigger.
Renders a `<tooltip-positioner>` custom element.

**Positioner Props:**

| Prop                  | Type                          | Default | Description |
| :-------------------- | :---------------------------- | :------ | :---------- |
| disableAnchorTracking | `boolean`                     | -       | -           |
| align                 | `Align`                       | -       | -           |
| alignOffset           | `number`                      | -       | -           |
| side                  | `Side`                        | -       | -           |
| sideOffset            | `number`                      | -       | -           |
| arrowPadding          | `number`                      | -       | -           |
| collisionAvoidance    | `'flip' \| 'shift' \| 'none'` | -       | -           |
| collisionPadding      | `number`                      | -       | -           |
| sticky                | `boolean`                     | -       | -           |
| positionMethod        | `'absolute' \| 'fixed'`       | -       | -           |
| children              | `unknown`                     | -       | -           |

**Positioner Data Attributes:**

| Attribute          | Type | Description |
| :----------------- | :--- | :---------- |
| data-open          | -    | -           |
| data-closed        | -    | -           |
| data-anchor-hidden | -    | -           |
| data-align         | -    | -           |
| data-side          | -    | -           |

### Positioner.Props

Re-export of [Positioner](#positioner) props.

### Positioner.State

```typescript
type PositionerState = { open: boolean; side: Side; align: Align; anchorHidden: boolean };
```

### Positioner.DataAttributes

Re-export of [Positioner](#positioner) data-attributes.

### Popup

A container for the tooltip contents.
Renders a `<tooltip-popup>` custom element.

**Popup Props:**

| Prop     | Type      | Default | Description |
| :------- | :-------- | :------ | :---------- |
| children | `unknown` | -       | -           |

**Popup Data Attributes:**

| Attribute           | Type | Description |
| :------------------ | :--- | :---------- |
| data-open           | -    | -           |
| data-closed         | -    | -           |
| data-starting-style | -    | -           |
| data-ending-style   | -    | -           |

### Popup.Props

Re-export of [Popup](#popup) props.

### Popup.State

```typescript
type PopupState = {
  transitionStatus: TransitionStatus;
  open: boolean;
  side: Side;
  align: Align;
  anchorHidden: boolean;
};
```

### Popup.DataAttributes

Re-export of [Popup](#popup) data-attributes.

### Arrow

An arrow that points toward the trigger.
Renders a `<tooltip-arrow>` custom element.

**Arrow Props:**

| Prop     | Type      | Default | Description |
| :------- | :-------- | :------ | :---------- |
| children | `unknown` | -       | -           |

**Arrow Data Attributes:**

| Attribute       | Type | Description |
| :-------------- | :--- | :---------- |
| data-open       | -    | -           |
| data-uncentered | -    | -           |
| data-align      | -    | -           |
| data-side       | -    | -           |

### Arrow.Props

Re-export of [Arrow](#arrow) props.

### Arrow.State

```typescript
type ArrowState = {
  uncentered: boolean;
  open: boolean;
  side: Side;
  align: Align;
  anchorHidden: boolean;
};
```

### Arrow.DataAttributes

Re-export of [Arrow](#arrow) data-attributes.

### createHandle

Creates a handle to connect a tooltip root with detached triggers.

**Return Value:**

```tsx
type ReturnValue = Handle<Payload>;
```

### Handle

Imperative handle shared between detached triggers and the tooltip root.

**Properties:**

| Property        | Type                   | Modifiers | Description |
| :-------------- | :--------------------- | :-------- | :---------- |
| isOpen          | `boolean`              | readonly  | -           |
| activeTriggerId | `string \| null`       | -         | -           |
| activePayload   | `Payload \| undefined` | -         | -           |

**Methods:**

```typescript
function open(_triggerId?: string | undefined): void;
```

```typescript
function close(): void;
```

## Additional Types

### Align

```typescript
type Align = 'start' | 'center' | 'end';
```

### ArrowApiProps

```typescript
type ArrowApiProps = { children?: unknown };
```

### PopupApiProps

```typescript
type PopupApiProps = { children?: unknown };
```

### PortalApiProps

```typescript
type PortalApiProps = { container?: HTMLElement | ShadowRoot | null; children?: unknown };
```

### PositionerApiProps

```typescript
type PositionerApiProps = {
  side?: Side;
  sideOffset?: number;
  align?: Align;
  alignOffset?: number;
  collisionAvoidance?: 'flip' | 'shift' | 'none';
  collisionPadding?: number;
  arrowPadding?: number;
  sticky?: boolean;
  disableAnchorTracking?: boolean;
  positionMethod?: 'absolute' | 'fixed';
  children?: unknown;
};
```

### ProviderApiProps

```typescript
type ProviderApiProps = {
  delay?: number | null;
  closeDelay?: number | null;
  timeout?: number;
  children?: unknown;
};
```

### RootApiProps

```typescript
type RootApiProps<Payload = unknown> = {
  defaultOpen?: boolean;
  open?: boolean;
  onOpenChange?: (open: boolean, details: Root.ChangeEventDetails) => void;
  onOpenChangeComplete?: (open: boolean) => void;
  handle?: Handle<Payload>;
  triggerId?: string | null;
  defaultTriggerId?: string | null;
  actionsRef?: { current: Root.Actions | null };
  disabled?: boolean;
  disableHoverablePopup?: boolean;
  children?: unknown;
};
```

### Side

```typescript
type Side = 'top' | 'right' | 'bottom' | 'left';
```

### TooltipDocumentation

```typescript
type TooltipDocumentation = {
  Provider: React.FC<ProviderApiProps>;
  Root: React.FC<RootApiProps>;
  Trigger: React.FC<TriggerApiProps>;
  Portal: React.FC<PortalApiProps>;
  Positioner: React.FC<PositionerApiProps>;
  Popup: React.FC<PopupApiProps>;
  Arrow: React.FC<ArrowApiProps>;
  createHandle: createHandle;
  Handle: unknown;
};
```

### TransitionStatus

```typescript
type TransitionStatus = 'starting' | 'ending';
```

### TriggerApiProps

```typescript
type TriggerApiProps<Payload = unknown> = {
  closeOnClick?: boolean;
  handle?: Handle<Payload>;
  payload?: Payload;
  disabled?: boolean;
  delay?: number | null;
  closeDelay?: number | null;
  id?: string;
  children?: unknown;
};
```

## External Types

### cancel

```typescript
type cancel = () => void;
```

### allowPropagation

```typescript
type allowPropagation = () => void;
```

### close

```typescript
type close = () => void;
```

### unmount

```typescript
type unmount = () => void;
```

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `Root.Actions`: `RootActions`
- `Root.ChangeEventReason`: `RootChangeEventReason`
- `Root.ChangeEventDetails`: `RootChangeEventDetails`
- `Trigger.State`: `TriggerState`
- `Trigger.DataAttributes`: `TriggerDataAttributes`
- `Positioner.State`: `PositionerState`
- `Positioner.DataAttributes`: `PositionerDataAttributes`
- `Popup.State`: `PopupState`
- `Popup.DataAttributes`: `PopupDataAttributes`
- `Arrow.State`: `ArrowState`
- `Arrow.DataAttributes`: `ArrowDataAttributes`
