# Checkbox Group

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/components/checkbox-group"'

## API Reference

### CheckboxGroup

Provides a shared state to a series of checkboxes.
Renders a `<div>` element by default.

**CheckboxGroup Props:**

| Prop          | Type                                                                                   | Default | Description                                                                                                                                                                                                              |
| :------------ | :------------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| defaultValue  | `string[]`                                                                             | -       | Default selected values for uncontrolled mode.                                                                                                                                                                           |
| value         | `string[]`                                                                             | -       | Controlled selected values.                                                                                                                                                                                              |
| onValueChange | `((value: string[], eventDetails: CheckboxGroup.ChangeEventDetails) => void)`          | -       | Callback fired when the selected values change.                                                                                                                                                                          |
| allValues     | `string[]`                                                                             | -       | All possible values of child checkboxes. Required for parent checkbox support.                                                                                                                                           |
| disabled      | `boolean`                                                                              | `false` | Whether the group is disabled.                                                                                                                                                                                           |
| render        | `((props: HTMLProps, state: CheckboxGroup.State) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with a template that has a single root element. Accepts a `TemplateResult` or a function that returns the template to render. |

**CheckboxGroup Data Attributes:**

| Attribute     | Type | Description |
| :------------ | :--- | :---------- |
| data-disabled | -    | -           |

### CheckboxGroup.Props

Re-export of [CheckboxGroup](#checkboxgroup) props.

### CheckboxGroup.State

```typescript
type CheckboxGroupState = {
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
};
```

### CheckboxGroup.ChangeEventDetails

```typescript
type CheckboxGroupChangeEventDetails = {
  event: Event;
  cancel: cancel;
  isCanceled: boolean;
  reason: 'none';
};
```

### CheckboxGroup.DataAttributes

Re-export of [CheckboxGroup](#checkboxgroup) data-attributes.

## Additional Types

### CheckboxGroupApiProps

```typescript
type CheckboxGroupApiProps = {
  /** Controlled selected values. */
  value?: string[];
  /** Default selected values for uncontrolled mode. */
  defaultValue?: string[];
  /** All possible values of child checkboxes. Required for parent checkbox support. */
  allValues?: string[];
  /**
   * Whether the group is disabled.
   * @default false
   */
  disabled?: boolean;
  /** Callback fired when the selected values change. */
  onValueChange?: (value: string[], eventDetails: CheckboxGroup.ChangeEventDetails) => void;
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with a template that has a single root element.
   *
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: CheckboxGroup.State) => TemplateResult) | TemplateResult;
};
```

## External Types

### cancel

```typescript
type cancel = () => void;
```

## Export Groups

- `CheckboxGroup`: `CheckboxGroupState`, `CheckboxGroupChangeEventDetails`, `CheckboxGroupDataAttributes`, `CheckboxGroupApiProps`, `CheckboxGroup`, `CheckboxGroup.Props`, `CheckboxGroup.State`, `CheckboxGroup.ChangeEventDetails`, `CheckboxGroup.DataAttributes`

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `CheckboxGroup.State`: `CheckboxGroupState`
- `CheckboxGroup.ChangeEventDetails`: `CheckboxGroupChangeEventDetails`
- `CheckboxGroup.DataAttributes`: `CheckboxGroupDataAttributes`
