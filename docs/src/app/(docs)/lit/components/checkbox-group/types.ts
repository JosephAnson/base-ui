import { createMultipleTypes } from 'docs/src/utils/createTypes';
import { CheckboxGroup } from './types-component';

export const { types: TypesCheckboxGroup } = createMultipleTypes(import.meta.url, {
  CheckboxGroup,
});
