# Popover

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/components/popover"'

## API Reference

### Root

Groups all parts of the popover.
Renders a `<popover-root>` custom element.

**Root Props:**

| Prop                    | Type                                                          | Default | Description |
| :---------------------- | :------------------------------------------------------------ | :------ | :---------- |
| defaultOpen             | `boolean`                                                     | -       | -           |
| open                    | `boolean`                                                     | -       | -           |
| onOpenChange            | `((open: boolean, details: Root.ChangeEventDetails) => void)` | -       | -           |
| actionsRef              | `{ current: Root.Actions \| null }`                           | -       | -           |
| defaultTriggerId        | `string \| null`                                              | -       | -           |
| disablePointerDismissal | `boolean`                                                     | -       | -           |
| handle                  | `Handle`                                                      | -       | -           |
| modal                   | `boolean`                                                     | -       | -           |
| onOpenChangeComplete    | `((open: boolean) => void)`                                   | -       | -           |
| triggerId               | `string \| null`                                              | -       | -           |

### Root.Props

Re-export of [Root](#root) props.

### Root.Actions

```typescript
type RootActions = { close: close; unmount: unmount };
```

### Root.ChangeEventReason

```typescript
type RootChangeEventReason =
  | 'trigger-hover'
  | 'trigger-focus'
  | 'trigger-press'
  | 'outside-press'
  | 'escape-key'
  | 'close-press'
  | 'focus-out'
  | 'imperative-action'
  | 'none';
```

### Root.ChangeEventDetails

```typescript
type RootChangeEventDetails = {
  reason: Root.ChangeEventReason;
  event: Event;
  cancel: cancel;
  allowPropagation: allowPropagation;
  isCanceled: boolean;
  isPropagationAllowed: boolean;
  trigger: Element | undefined;
};
```

### Trigger

A button-like trigger that opens the popover.
Renders a `<popover-trigger>` custom element.

**Trigger Props:**

| Prop         | Type                                                                                           | Default | Description |
| :----------- | :--------------------------------------------------------------------------------------------- | :------ | :---------- |
| handle       | `Handle`                                                                                       | -       | -           |
| nativeButton | `boolean`                                                                                      | -       | -           |
| payload      | `unknown`                                                                                      | -       | -           |
| disabled     | `boolean`                                                                                      | -       | -           |
| openOnHover  | `boolean`                                                                                      | -       | -           |
| delay        | `number`                                                                                       | -       | -           |
| closeDelay   | `number`                                                                                       | -       | -           |
| id           | `string`                                                                                       | -       | -           |
| render       | `((props: Record<string, unknown>, state: Trigger.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Trigger Data Attributes:**

| Attribute       | Type | Description |
| :-------------- | :--- | :---------- |
| data-popup-open | -    | -           |

### Trigger.Props

Re-export of [Trigger](#trigger) props.

### Trigger.State

```typescript
type TriggerState = { disabled: boolean; open: boolean };
```

### Trigger.DataAttributes

Re-export of [Trigger](#trigger) data-attributes.

### Portal

A portal wrapper for the positioned popup subtree.
By default, the portal element is appended to `<body>`.
Renders a `<popover-portal>` custom element.

**Portal Props:**

| Prop      | Type                                                                                          | Default | Description |
| :-------- | :-------------------------------------------------------------------------------------------- | :------ | :---------- |
| container | `HTMLElement \| ShadowRoot \| null`                                                           | -       | -           |
| render    | `((props: Record<string, unknown>, state: Portal.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Portal Data Attributes:**

| Attribute   | Type | Description |
| :---------- | :--- | :---------- |
| data-open   | -    | -           |
| data-closed | -    | -           |

### Portal.Props

Re-export of [Portal](#portal) props.

### Portal.State

```typescript
type PortalState = { open: boolean };
```

### Portal.DataAttributes

Re-export of [Portal](#portal) data-attributes.

### Backdrop

A backdrop rendered beneath the popup.
Renders a `<popover-backdrop>` custom element.

**Backdrop Props:**

| Prop   | Type                                                                                            | Default | Description |
| :----- | :---------------------------------------------------------------------------------------------- | :------ | :---------- |
| render | `((props: Record<string, unknown>, state: Backdrop.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Backdrop Data Attributes:**

| Attribute   | Type | Description |
| :---------- | :--- | :---------- |
| data-open   | -    | -           |
| data-closed | -    | -           |

### Backdrop.Props

Re-export of [Backdrop](#backdrop) props.

### Backdrop.State

```typescript
type BackdropState = { open: boolean };
```

### Backdrop.DataAttributes

Re-export of [Backdrop](#backdrop) data-attributes.

### Positioner

Positions the popup relative to the active trigger.
Renders a `<popover-positioner>` custom element.

**Positioner Props:**

| Prop                  | Type                                                                                              | Default | Description |
| :-------------------- | :------------------------------------------------------------------------------------------------ | :------ | :---------- |
| disableAnchorTracking | `boolean`                                                                                         | -       | -           |
| align                 | `Align`                                                                                           | -       | -           |
| alignOffset           | `number`                                                                                          | -       | -           |
| side                  | `Side`                                                                                            | -       | -           |
| sideOffset            | `number`                                                                                          | -       | -           |
| arrowPadding          | `number`                                                                                          | -       | -           |
| anchor                | `Element`                                                                                         | -       | -           |
| collisionAvoidance    | `'flip' \| 'shift' \| 'none'`                                                                     | -       | -           |
| collisionBoundary     | `'clipping-ancestors' \| Element \| Element[] \| null`                                            | -       | -           |
| collisionPadding      | `number`                                                                                          | -       | -           |
| sticky                | `boolean`                                                                                         | -       | -           |
| positionMethod        | `'absolute' \| 'fixed'`                                                                           | -       | -           |
| render                | `((props: Record<string, unknown>, state: Positioner.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Positioner Data Attributes:**

| Attribute          | Type | Description |
| :----------------- | :--- | :---------- |
| data-open          | -    | -           |
| data-closed        | -    | -           |
| data-anchor-hidden | -    | -           |
| data-align         | -    | -           |
| data-side          | -    | -           |

**Positioner CSS Variables:**

| Variable             | Type | Description |
| :------------------- | :--- | :---------- |
| `--transform-origin` | ``   | -           |

### Positioner.Props

Re-export of [Positioner](#positioner) props.

### Positioner.State

```typescript
type PositionerState = { side: Side; align: Align; anchorHidden: boolean; open: boolean };
```

### Positioner.DataAttributes

Re-export of [Positioner](#positioner) data-attributes.

### Positioner.CssVars

Re-export of [Positioner](#positioner) css-variables.

### Popup

The popup surface itself.
Renders a `<popover-popup>` custom element.

**Popup Props:**

| Prop   | Type                                                                                         | Default | Description |
| :----- | :------------------------------------------------------------------------------------------- | :------ | :---------- |
| render | `((props: Record<string, unknown>, state: Popup.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Popup Data Attributes:**

| Attribute           | Type | Description |
| :------------------ | :--- | :---------- |
| data-open           | -    | -           |
| data-closed         | -    | -           |
| data-starting-style | -    | -           |
| data-ending-style   | -    | -           |

### Popup.Props

Re-export of [Popup](#popup) props.

### Popup.State

```typescript
type PopupState = {
  transitionStatus: TransitionStatus;
  modal: boolean;
  side: Side;
  align: Align;
  anchorHidden: boolean;
  open: boolean;
};
```

### Popup.DataAttributes

Re-export of [Popup](#popup) data-attributes.

### Arrow

An arrow pointing from the popup toward the trigger.
Renders a `<popover-arrow>` custom element.

**Arrow Props:**

| Prop   | Type                                                                                         | Default | Description |
| :----- | :------------------------------------------------------------------------------------------- | :------ | :---------- |
| render | `((props: Record<string, unknown>, state: Arrow.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Arrow Data Attributes:**

| Attribute       | Type | Description |
| :-------------- | :--- | :---------- |
| data-open       | -    | -           |
| data-uncentered | -    | -           |
| data-align      | -    | -           |
| data-side       | -    | -           |

### Arrow.Props

Re-export of [Arrow](#arrow) props.

### Arrow.State

```typescript
type ArrowState = {
  uncentered: boolean;
  side: Side;
  align: Align;
  anchorHidden: boolean;
  open: boolean;
};
```

### Arrow.DataAttributes

Re-export of [Arrow](#arrow) data-attributes.

### Title

Labels the popup for assistive technology.
Renders a `<popover-title>` custom element.

**Title Props:**

| Prop   | Type                                                                                                   | Default | Description |
| :----- | :----------------------------------------------------------------------------------------------------- | :------ | :---------- |
| render | `((props: Record<string, unknown>, state: Record<string, never>) => TemplateResult) \| TemplateResult` | -       | -           |

### Title.Props

Re-export of [Title](#title) props.

### Description

Describes the popup for assistive technology.
Renders a `<popover-description>` custom element.

**Description Props:**

| Prop   | Type                                                                                                   | Default | Description |
| :----- | :----------------------------------------------------------------------------------------------------- | :------ | :---------- |
| render | `((props: Record<string, unknown>, state: Record<string, never>) => TemplateResult) \| TemplateResult` | -       | -           |

### Description.Props

Re-export of [Description](#description) props.

### Close

Closes the popover when pressed.
Renders a `<popover-close>` custom element.

**Close Props:**

| Prop         | Type                                                                                         | Default | Description |
| :----------- | :------------------------------------------------------------------------------------------- | :------ | :---------- |
| nativeButton | `boolean`                                                                                    | -       | -           |
| disabled     | `boolean`                                                                                    | -       | -           |
| render       | `((props: Record<string, unknown>, state: Close.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Close Data Attributes:**

| Attribute     | Type | Description |
| :------------ | :--- | :---------- |
| data-disabled | -    | -           |

### Close.Props

Re-export of [Close](#close) props.

### Close.State

```typescript
type CloseState = { disabled: boolean };
```

### Close.DataAttributes

Re-export of [Close](#close) data-attributes.

### Viewport

Wraps popup content and exposes transition-oriented data attributes.
Renders a `<popover-viewport>` custom element.

**Viewport Props:**

| Prop   | Type                                                                                            | Default | Description |
| :----- | :---------------------------------------------------------------------------------------------- | :------ | :---------- |
| render | `((props: Record<string, unknown>, state: Viewport.State) => TemplateResult) \| TemplateResult` | -       | -           |

**Viewport Data Attributes:**

| Attribute                 | Type | Description |
| :------------------------ | :--- | :---------- |
| data-activation-direction | -    | -           |
| data-instant              | -    | -           |
| data-transitioning        | -    | -           |

**Viewport CSS Variables:**

| Variable         | Type | Description |
| :--------------- | :--- | :---------- |
| `--popup-height` | ``   | -           |
| `--popup-width`  | ``   | -           |

### Viewport.Props

Re-export of [Viewport](#viewport) props.

### Viewport.State

```typescript
type ViewportState = {
  activationDirection: string | undefined;
  transitioning: boolean;
  instant: 'dismiss' | 'click' | undefined;
};
```

### Viewport.DataAttributes

Re-export of [Viewport](#viewport) data-attributes.

### Viewport.CssVars

Re-export of [Viewport](#viewport) css-variables.

### createHandle

Creates a handle that connects detached triggers to a popover root.

**Return Value:**

```tsx
type ReturnValue = Handle;
```

### Handle

Imperative handle shared between detached triggers and the popover root.

**Properties:**

| Property | Type      | Modifiers | Description |
| :------- | :-------- | :-------- | :---------- |
| isOpen   | `boolean` | readonly  | -           |

**Methods:**

```typescript
function open(_triggerId?: string | undefined): void;
```

```typescript
function close(): void;
```

## Additional Types

### Align

```typescript
type Align = 'start' | 'center' | 'end';
```

### ArrowApiProps

```typescript
type ArrowApiProps = {
  render?:
    | ((props: Record<string, unknown>, state: Arrow.State) => TemplateResult)
    | TemplateResult;
};
```

### BackdropApiProps

```typescript
type BackdropApiProps = {
  render?:
    | ((props: Record<string, unknown>, state: Backdrop.State) => TemplateResult)
    | TemplateResult;
};
```

### CloseApiProps

```typescript
type CloseApiProps = {
  disabled?: boolean;
  nativeButton?: boolean;
  render?:
    | ((props: Record<string, unknown>, state: Close.State) => TemplateResult)
    | TemplateResult;
};
```

### DescriptionApiProps

```typescript
type DescriptionApiProps = {
  render?:
    | ((props: Record<string, unknown>, state: Record<string, never>) => TemplateResult)
    | TemplateResult;
};
```

### PopoverDocumentation

```typescript
type PopoverDocumentation = {
  Root: React.FC<RootApiProps>;
  Trigger: React.FC<TriggerApiProps>;
  Portal: React.FC<PortalApiProps>;
  Positioner: React.FC<PositionerApiProps>;
  Popup: React.FC<PopupApiProps>;
  Arrow: React.FC<ArrowApiProps>;
  Backdrop: React.FC<BackdropApiProps>;
  Title: React.FC<TitleApiProps>;
  Description: React.FC<DescriptionApiProps>;
  Close: React.FC<CloseApiProps>;
  Viewport: React.FC<ViewportApiProps>;
  createHandle: createHandle;
  Handle: unknown;
};
```

### PopupApiProps

```typescript
type PopupApiProps = {
  render?:
    | ((props: Record<string, unknown>, state: Popup.State) => TemplateResult)
    | TemplateResult;
};
```

### PortalApiProps

```typescript
type PortalApiProps = {
  container?: HTMLElement | ShadowRoot | null;
  render?:
    | ((props: Record<string, unknown>, state: Portal.State) => TemplateResult)
    | TemplateResult;
};
```

### PositionerApiProps

```typescript
type PositionerApiProps = {
  side?: Side;
  sideOffset?: number;
  align?: Align;
  alignOffset?: number;
  collisionAvoidance?: 'flip' | 'shift' | 'none';
  collisionBoundary?: 'clipping-ancestors' | Element | Element[] | null;
  collisionPadding?: number;
  arrowPadding?: number;
  sticky?: boolean;
  disableAnchorTracking?: boolean;
  positionMethod?: 'absolute' | 'fixed';
  anchor?: Element;
  render?:
    | ((props: Record<string, unknown>, state: Positioner.State) => TemplateResult)
    | TemplateResult;
};
```

### RootApiProps

```typescript
type RootApiProps = {
  defaultOpen?: boolean;
  open?: boolean;
  onOpenChange?: (open: boolean, details: Root.ChangeEventDetails) => void;
  onOpenChangeComplete?: (open: boolean) => void;
  actionsRef?: { current: Root.Actions | null };
  modal?: boolean;
  disablePointerDismissal?: boolean;
  handle?: Handle;
  triggerId?: string | null;
  defaultTriggerId?: string | null;
};
```

### Side

```typescript
type Side = 'top' | 'right' | 'bottom' | 'left';
```

### TitleApiProps

```typescript
type TitleApiProps = {
  render?:
    | ((props: Record<string, unknown>, state: Record<string, never>) => TemplateResult)
    | TemplateResult;
};
```

### TransitionStatus

```typescript
type TransitionStatus = 'starting' | 'ending';
```

### TriggerApiProps

```typescript
type TriggerApiProps = {
  disabled?: boolean;
  nativeButton?: boolean;
  openOnHover?: boolean;
  delay?: number;
  closeDelay?: number;
  handle?: Handle;
  payload?: unknown;
  id?: string;
  render?:
    | ((props: Record<string, unknown>, state: Trigger.State) => TemplateResult)
    | TemplateResult;
};
```

### ViewportApiProps

```typescript
type ViewportApiProps = {
  render?:
    | ((props: Record<string, unknown>, state: Viewport.State) => TemplateResult)
    | TemplateResult;
};
```

## External Types

### cancel

```typescript
type cancel = () => void;
```

### allowPropagation

```typescript
type allowPropagation = () => void;
```

### close

```typescript
type close = () => void;
```

### unmount

```typescript
type unmount = () => void;
```

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `Root.Actions`: `RootActions`
- `Root.ChangeEventReason`: `RootChangeEventReason`
- `Root.ChangeEventDetails`: `RootChangeEventDetails`
- `Trigger.State`: `TriggerState`
- `Trigger.DataAttributes`: `TriggerDataAttributes`
- `Portal.State`: `PortalState`
- `Portal.DataAttributes`: `PortalDataAttributes`
- `Positioner.State`: `PositionerState`
- `Positioner.DataAttributes`: `PositionerDataAttributes`
- `Positioner.CssVars`: `PositionerCssVars`
- `Popup.State`: `PopupState`
- `Popup.DataAttributes`: `PopupDataAttributes`
- `Arrow.State`: `ArrowState`
- `Arrow.DataAttributes`: `ArrowDataAttributes`
- `Backdrop.State`: `BackdropState`
- `Backdrop.DataAttributes`: `BackdropDataAttributes`
- `Close.State`: `CloseState`
- `Close.DataAttributes`: `CloseDataAttributes`
- `Viewport.State`: `ViewportState`
- `Viewport.DataAttributes`: `ViewportDataAttributes`
- `Viewport.CssVars`: `ViewportCssVars`
