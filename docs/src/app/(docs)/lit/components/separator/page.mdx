# Separator

<Subtitle>A separator element accessible to screen readers.</Subtitle>
<Meta
  name="description"
  content="A high-quality, unstyled separator component that is accessible to screen readers."
/>

import { DemoSeparatorHero } from './demos/hero';

<DemoSeparatorHero />

## Anatomy

Import the element and use it as a single part:

```html title="Anatomy"
<script type="module">
  import '@base-ui/lit/separator';
</script>

<separator-root orientation="horizontal"></separator-root>
```

## API reference

import { TypesSeparator } from './types';

<TypesSeparator />

export const metadata = {
  keywords: [
    'Separator',
    'Divider Component',
    'Divider',
    'Horizontal Rule',
    'Horizontal Divider',
    'Vertical Divider',
    'Accessible Separator',
    'Screen Reader Separator',
    'Base UI',
  ],
};
