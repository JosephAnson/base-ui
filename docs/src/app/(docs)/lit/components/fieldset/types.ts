import { createMultipleTypes } from 'docs/src/utils/createTypes';
import { Fieldset } from './types-component';

export const { types: TypesFieldset } = createMultipleTypes(import.meta.url, Fieldset);
