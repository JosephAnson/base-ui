# Fieldset

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/components/fieldset"'

## API Reference

### Root

Groups a shared legend with related controls.
Renders a `<fieldset-root>` custom element.

### Root.Props

```typescript
type RootProps = {};
```

### Root.State

```typescript
type RootState = {
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
};
```

### Legend

An accessible label that is automatically associated with the fieldset.
Renders a `<fieldset-legend>` custom element.

### Legend.Props

```typescript
type LegendProps = {};
```

### Legend.State

```typescript
type LegendState = {
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
};
```

## Additional Types

### Fieldset

```typescript
type Fieldset = { Root: React.FC<RootApiProps>; Legend: React.FC<LegendApiProps> };
```

### LegendApiProps

```typescript
type LegendApiProps = {};
```

### RootApiProps

```typescript
type RootApiProps = {};
```

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `Root.State`: `RootState`
- `Legend.State`: `LegendState`
