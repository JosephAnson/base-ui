import { createMultipleTypes } from 'docs/src/utils/createTypes';
import { Collapsible } from './types-component';

export const { types: TypesCollapsible } = createMultipleTypes(import.meta.url, Collapsible);
