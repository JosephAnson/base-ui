# Toggle Group

<Subtitle>Provides a shared state to a series of toggle buttons.</Subtitle>
<Meta
  name="description"
  content="A high-quality, unstyled Lit toggle group component that provides shared state to a series of toggle buttons."
/>

import { DemoToggleGroupHero } from './demos/hero';
import { DemoToggleGroupMultiple } from './demos/multiple';
import { TypesToggleGroup } from './types';

<DemoToggleGroupHero />

## Anatomy

Import the custom elements and compose them as separate parts:

```html title="Anatomy"
<toggle-group-root>
  <toggle-root value="left">Left</toggle-root>
  <toggle-root value="center">Center</toggle-root>
  <toggle-root value="right">Right</toggle-root>
</toggle-group-root>
```

## Examples

### Multiple

Add the `multiple` attribute to allow pressing more than one toggle at a time.

<DemoToggleGroupMultiple />

## API reference

<TypesToggleGroup />

### ToggleGroup Data Attributes

| Attribute          | Type                         | Description                                                                                        |
| :----------------- | :--------------------------- | :------------------------------------------------------------------------------------------------- |
| `data-orientation` | `'horizontal' \| 'vertical'` | Indicates the orientation of the toggle group.                                                     |
| `data-disabled`    | -                            | Present when the toggle group is disabled.                                                         |
| `data-multiple`    | -                            | Present when the toggle group allows multiple buttons to be in the pressed state at the same time. |

export const metadata = {
  keywords: [
    'Lit Toggle Group',
    'Toggle Button Group',
    'Segmented Control',
    'Button Set',
    'Radio Group Alternative',
    'Exclusive Selection Toggle',
    'Multi Select Toggle Buttons',
    'Accessible Toggle Group',
    'Headless Lit Components',
    'Base UI',
  ],
};
