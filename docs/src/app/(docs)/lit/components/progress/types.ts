import { createMultipleTypes } from 'docs/src/utils/createTypes';
import { Progress } from './types-component';

export const { types: TypesProgress } = createMultipleTypes(import.meta.url, Progress);
