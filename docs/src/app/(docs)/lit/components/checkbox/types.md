# Checkbox

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/components/checkbox"'

## API Reference

### Root

Represents the checkbox itself.
Renders a `<span>` element and a hidden `<input>` beside by default.

**Root Props:**

| Prop            | Type                                                                          | Default | Description                                                                                                                                                                                                              |
| :-------------- | :---------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| name            | `string`                                                                      | -       | Shared form field name for form submission.                                                                                                                                                                              |
| defaultChecked  | `boolean`                                                                     | `false` | The uncontrolled checked state when initially rendered.                                                                                                                                                                  |
| checked         | `boolean`                                                                     | -       | Whether the checkbox is currently checked.                                                                                                                                                                               |
| onCheckedChange | `((checked: boolean, eventDetails: CheckboxRootChangeEventDetails) => void)`  | -       | Callback fired when the checked state changes.                                                                                                                                                                           |
| indeterminate   | `boolean`                                                                     | `false` | Whether the checkbox is in an indeterminate state.                                                                                                                                                                       |
| value           | `string`                                                                      | `'on'`  | The checked form value.                                                                                                                                                                                                  |
| form            | `string`                                                                      | -       | Associates the hidden input with an external form element.                                                                                                                                                               |
| nativeButton    | `boolean`                                                                     | `false` | Whether the rendered element should be treated as a native `<button>`.&#xA;Set to `true` when replacing the default element with a native button via `render`.                                                           |
| parent          | `boolean`                                                                     | `false` | Whether this checkbox acts as a parent "select all" checkbox inside a group.                                                                                                                                             |
| uncheckedValue  | `string`                                                                      | -       | The unchecked form value.                                                                                                                                                                                                |
| disabled        | `boolean`                                                                     | `false` | Whether the component should ignore user interaction.                                                                                                                                                                    |
| readOnly        | `boolean`                                                                     | `false` | Whether the user should be unable to check or uncheck the checkbox.                                                                                                                                                      |
| required        | `boolean`                                                                     | `false` | Whether the user must check the checkbox before submitting a form.                                                                                                                                                       |
| inputRef        | `React.Ref<HTMLInputElement>`                                                 | -       | A ref to access the hidden input element.                                                                                                                                                                                |
| id              | `string`                                                                      | -       | The id of the checkbox element.                                                                                                                                                                                          |
| render          | `((props: HTMLProps, state: Root.State) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with a template that has a single root element. Accepts a `TemplateResult` or a function that returns the template to render. |

**Root Data Attributes:**

| Attribute          | Type | Description |
| :----------------- | :--- | :---------- |
| data-checked       | -    | -           |
| data-unchecked     | -    | -           |
| data-disabled      | -    | -           |
| data-readonly      | -    | -           |
| data-required      | -    | -           |
| data-indeterminate | -    | -           |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type RootState = {
  /** Whether the checkbox is currently checked. */
  checked: boolean;
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** Whether the checkbox is in an indeterminate state. */
  indeterminate: boolean;
  /** Whether the user should be unable to check or uncheck the checkbox. */
  readOnly: boolean;
  /** Whether the user must check the checkbox before submitting a form. */
  required: boolean;
};
```

### Root.ChangeEventDetails

```typescript
type RootChangeEventDetails = { event: Event; cancel: cancel; isCanceled: boolean; reason: 'none' };
```

### Root.DataAttributes

Re-export of [Root](#root) data-attributes.

### Indicator

Indicates whether the checkbox is checked.
Renders a `<span>` element by default.

**Indicator Props:**

| Prop        | Type                                                                               | Default | Description                                                                                                                                                                                                              |
| :---------- | :--------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| keepMounted | `boolean`                                                                          | `false` | Whether the indicator should stay mounted when unchecked.                                                                                                                                                                |
| render      | `((props: HTMLProps, state: Indicator.State) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element&#xA;with a different tag, or compose it with a template that has a single root element. Accepts a `TemplateResult` or a function that returns the template to render. |

**Indicator Data Attributes:**

| Attribute          | Type | Description |
| :----------------- | :--- | :---------- |
| data-checked       | -    | -           |
| data-unchecked     | -    | -           |
| data-disabled      | -    | -           |
| data-readonly      | -    | -           |
| data-required      | -    | -           |
| data-indeterminate | -    | -           |

### Indicator.Props

Re-export of [Indicator](#indicator) props.

### Indicator.State

```typescript
type IndicatorState = {
  /** Whether the checkbox is currently checked. */
  checked: boolean;
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** Whether the checkbox is in an indeterminate state. */
  indeterminate: boolean;
  /** Whether the user should be unable to check or uncheck the checkbox. */
  readOnly: boolean;
  /** Whether the user must check the checkbox before submitting a form. */
  required: boolean;
};
```

### Indicator.DataAttributes

Re-export of [Indicator](#indicator) data-attributes.

## Additional Types

### Checkbox

```typescript
type Checkbox = { Root: React.FC<RootApiProps>; Indicator: React.FC<IndicatorApiProps> };
```

### CheckboxRootChangeEventDetails

```typescript
type CheckboxRootChangeEventDetails = {
  event: Event;
  cancel: cancel;
  isCanceled: boolean;
  reason: 'none';
};
```

### IndicatorApiProps

```typescript
type IndicatorApiProps = {
  /**
   * Whether the indicator should stay mounted when unchecked.
   * @default false
   */
  keepMounted?: boolean;
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with a template that has a single root element.
   *
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Indicator.State) => TemplateResult) | TemplateResult;
};
```

### RootApiProps

```typescript
type RootApiProps = {
  /** Whether the checkbox is currently checked. */
  checked?: boolean;
  /**
   * The uncontrolled checked state when initially rendered.
   * @default false
   */
  defaultChecked?: boolean;
  /**
   * Whether the component should ignore user interaction.
   * @default false
   */
  disabled?: boolean;
  /** Associates the hidden input with an external form element. */
  form?: string;
  /** The id of the checkbox element. */
  id?: string;
  /**
   * Whether the checkbox is in an indeterminate state.
   * @default false
   */
  indeterminate?: boolean;
  /** A ref to access the hidden input element. */
  inputRef?: React.Ref<HTMLInputElement>;
  /** Shared form field name for form submission. */
  name?: string;
  /**
   * Whether the rendered element should be treated as a native `<button>`.
   * Set to `true` when replacing the default element with a native button via `render`.
   * @default false
   */
  nativeButton?: boolean;
  /** Callback fired when the checked state changes. */
  onCheckedChange?: (checked: boolean, eventDetails: CheckboxRootChangeEventDetails) => void;
  /**
   * Whether this checkbox acts as a parent "select all" checkbox inside a group.
   * @default false
   */
  parent?: boolean;
  /**
   * Whether the user should be unable to check or uncheck the checkbox.
   * @default false
   */
  readOnly?: boolean;
  /**
   * Allows you to replace the component's HTML element
   * with a different tag, or compose it with a template that has a single root element.
   *
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Root.State) => TemplateResult) | TemplateResult;
  /**
   * Whether the user must check the checkbox before submitting a form.
   * @default false
   */
  required?: boolean;
  /** The unchecked form value. */
  uncheckedValue?: string;
  /**
   * The checked form value.
   * @default 'on'
   */
  value?: string;
};
```

## External Types

### cancel

```typescript
type cancel = () => void;
```

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `Root.State`: `RootState`
- `Root.DataAttributes`: `RootDataAttributes`
- `Indicator.State`: `IndicatorState`
- `Indicator.DataAttributes`: `IndicatorDataAttributes`
