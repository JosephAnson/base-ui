# Accordion

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate "(docs)/lit/components/accordion"'

## API Reference

### Root

Groups all parts of the accordion.
Renders an `<accordion-root>` custom element.

**Root Props:**

| Prop             | Type                                                                                 | Default      | Description                                                                                                                                                                                                     |
| :--------------- | :----------------------------------------------------------------------------------- | :----------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| defaultValue     | `any[]`                                                                              | -            | The uncontrolled value of the item(s) that should be initially expanded.                                                                                                                                        |
| value            | `any[]`                                                                              | -            | The controlled value of the item(s) that should be expanded.                                                                                                                                                    |
| onValueChange    | `((value: any[], eventDetails: Root.ChangeEventDetails) => void)`                    | -            | Event handler called when an accordion item is expanded or collapsed.                                                                                                                                           |
| hiddenUntilFound | `boolean`                                                                            | `false`      | Allows the browser's built-in page search to find and expand the panel contents.&#xA;Overrides the `keepMounted` prop and uses `hidden="until-found"`&#xA;to hide the element without removing it from the DOM. |
| loopFocus        | `boolean`                                                                            | `true`       | Whether to loop keyboard focus back to the first item&#xA;when the end of the list is reached while using the arrow keys.                                                                                       |
| multiple         | `boolean`                                                                            | `false`      | Whether multiple items can be open at the same time.                                                                                                                                                            |
| disabled         | `boolean`                                                                            | `false`      | Whether the component should ignore user interaction.                                                                                                                                                           |
| orientation      | `Orientation`                                                                        | `'vertical'` | The visual orientation of the accordion.                                                                                                                                                                        |
| keepMounted      | `boolean`                                                                            | `false`      | Whether to keep the element in the DOM while the panel is closed.&#xA;This prop is ignored when `hiddenUntilFound` is used.                                                                                     |
| render           | `((props: HTMLProps, state: Root.State<Value>) => TemplateResult) \| TemplateResult` | -            | Allows you to replace the component's HTML element with a custom template.&#xA;Accepts a `TemplateResult` or a function that returns the template to render.                                                    |

**Root Data Attributes:**

| Attribute        | Type | Description |
| :--------------- | :--- | :---------- |
| data-orientation | -    | -           |
| data-disabled    | -    | -           |

### Root.Props

Re-export of [Root](#root) props.

### Root.State

```typescript
type RootState<Value = any> = {
  /** The current value. */
  value: Value[];
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** The component orientation. */
  orientation: Orientation;
};
```

### Root.ChangeEventReason

```typescript
type RootChangeEventReason = 'trigger-press' | 'none';
```

### Root.ChangeEventDetails

```typescript
type RootChangeEventDetails = {
  /** The reason for the event. */
  reason: Root.ChangeEventReason;
  /** The native event associated with the change. */
  event: Event;
  /** Cancels Base UI from handling the event. */
  cancel: () => void;
  /** Allows the event to propagate in cases where Base UI will stop the propagation. */
  allowPropagation: () => void;
  /** Indicates whether the event has been canceled. */
  isCanceled: boolean;
  /** Indicates whether the event is allowed to propagate. */
  isPropagationAllowed: boolean;
  /** The element that triggered the event, if applicable. */
  trigger: Element | undefined;
};
```

### Root.DataAttributes

Re-export of [Root](#root) data-attributes.

### Trigger

A button that opens and closes the corresponding panel.
Renders an `<accordion-trigger>` custom element.

**Trigger Props:**

| Prop         | Type                                                                                    | Default | Description                                                                                                                                                        |
| :----------- | :-------------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| nativeButton | `boolean`                                                                               | `true`  | Whether the component renders a native `<button>` element when replacing it&#xA;via the `render` prop.&#xA;Set to `false` if the rendered element is not a button. |
| render       | `((props: HTMLProps, state: Trigger.State<Value>) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element with a custom template.&#xA;Accepts a `TemplateResult` or a function that returns the template to render.       |

**Trigger Data Attributes:**

| Attribute       | Type | Description |
| :-------------- | :--- | :---------- |
| data-panel-open | -    | -           |
| data-disabled   | -    | -           |

### Trigger.Props

Re-export of [Trigger](#trigger) props.

### Trigger.State

```typescript
type TriggerState<Value = any> = {
  /** The item index. */
  index: number;
  /** Whether the component is open. */
  open: boolean;
  /** The current value. */
  value: Value[];
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** The component orientation. */
  orientation: Orientation;
};
```

### Trigger.DataAttributes

Re-export of [Trigger](#trigger) data-attributes.

### Item

Groups an accordion header with the corresponding panel.
Renders an `<accordion-item>` custom element.

**Item Props:**

| Prop         | Type                                                                                 | Default | Description                                                                                                                                                  |
| :----------- | :----------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| value        | `Value`                                                                              | -       | A unique value that identifies this accordion item.                                                                                                          |
| onOpenChange | `((open: boolean, eventDetails: Item.ChangeEventDetails) => void)`                   | -       | Event handler called when the panel is opened or closed.                                                                                                     |
| disabled     | `boolean`                                                                            | `false` | Whether the component should ignore user interaction.                                                                                                        |
| render       | `((props: HTMLProps, state: Item.State<Value>) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element with a custom template.&#xA;Accepts a `TemplateResult` or a function that returns the template to render. |

**Item Data Attributes:**

| Attribute     | Type | Description |
| :------------ | :--- | :---------- |
| data-open     | -    | -           |
| data-disabled | -    | -           |
| data-index    | -    | -           |

### Item.Props

Re-export of [Item](#item) props.

### Item.State

```typescript
type ItemState<Value = any> = {
  /** The item index. */
  index: number;
  /** Whether the component is open. */
  open: boolean;
  /** The current value. */
  value: Value[];
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** The component orientation. */
  orientation: Orientation;
};
```

### Item.ChangeEventReason

```typescript
type ItemChangeEventReason = 'trigger-press' | 'none';
```

### Item.ChangeEventDetails

```typescript
type ItemChangeEventDetails = {
  reason: Item.ChangeEventReason;
  /** The native event associated with the change. */
  event: Event;
  /** Cancels Base UI from handling the event. */
  cancel: () => void;
  /** Allows the event to propagate in cases where Base UI will stop the propagation. */
  allowPropagation: () => void;
  /** Indicates whether the event has been canceled. */
  isCanceled: boolean;
  /** Indicates whether the event is allowed to propagate. */
  isPropagationAllowed: boolean;
  /** The element that triggered the event, if applicable. */
  trigger: Element | undefined;
};
```

### Item.DataAttributes

Re-export of [Item](#item) data-attributes.

### Header

A heading that labels the corresponding panel.
Renders an `<accordion-header>` custom element.

**Header Props:**

| Prop   | Type                                                                                   | Default | Description                                                                                                                                                  |
| :----- | :------------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| render | `((props: HTMLProps, state: Header.State<Value>) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element with a custom template.&#xA;Accepts a `TemplateResult` or a function that returns the template to render. |

**Header Data Attributes:**

| Attribute     | Type | Description |
| :------------ | :--- | :---------- |
| data-open     | -    | -           |
| data-disabled | -    | -           |
| data-index    | -    | -           |

### Header.Props

Re-export of [Header](#header) props.

### Header.State

```typescript
type HeaderState<Value = any> = {
  /** The item index. */
  index: number;
  /** Whether the component is open. */
  open: boolean;
  /** The current value. */
  value: Value[];
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** The component orientation. */
  orientation: Orientation;
};
```

### Header.DataAttributes

Re-export of [Header](#header) data-attributes.

### Panel

A collapsible panel with the accordion item contents.
Renders an `<accordion-panel>` custom element.

**Panel Props:**

| Prop             | Type                                                                                  | Default | Description                                                                                                                                                                                                     |
| :--------------- | :------------------------------------------------------------------------------------ | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| hiddenUntilFound | `boolean`                                                                             | `false` | Allows the browser's built-in page search to find and expand the panel contents.&#xA;Overrides the `keepMounted` prop and uses `hidden="until-found"`&#xA;to hide the element without removing it from the DOM. |
| keepMounted      | `boolean`                                                                             | `false` | Whether to keep the element in the DOM while the panel is hidden.&#xA;This prop is ignored when `hiddenUntilFound` is used.                                                                                     |
| render           | `((props: HTMLProps, state: Panel.State<Value>) => TemplateResult) \| TemplateResult` | -       | Allows you to replace the component's HTML element with a custom template.&#xA;Accepts a `TemplateResult` or a function that returns the template to render.                                                    |

**Panel Data Attributes:**

| Attribute           | Type | Description |
| :------------------ | :--- | :---------- |
| data-open           | -    | -           |
| data-disabled       | -    | -           |
| data-index          | -    | -           |
| data-starting-style | -    | -           |
| data-ending-style   | -    | -           |

**Panel CSS Variables:**

| Variable                   | Type | Description |
| :------------------------- | :--- | :---------- |
| `--accordion-panel-height` | ``   | -           |
| `--accordion-panel-width`  | ``   | -           |

### Panel.Props

Re-export of [Panel](#panel) props.

### Panel.State

```typescript
type PanelState<Value = any> = {
  /** The transition status of the component. */
  transitionStatus: TransitionStatus;
  /** The item index. */
  index: number;
  /** Whether the component is open. */
  open: boolean;
  /** The current value. */
  value: Value[];
  /** Whether the component should ignore user interaction. */
  disabled: boolean;
  /** The component orientation. */
  orientation: Orientation;
};
```

### Panel.DataAttributes

Re-export of [Panel](#panel) data-attributes.

### Panel.CssVars

Re-export of [Panel](#panel) css-variables.

## Additional Types

### Orientation

```typescript
type Orientation = 'horizontal' | 'vertical';
```

### Accordion

```typescript
type Accordion = {
  Root: React.FC<RootApiProps>;
  Item: React.FC<ItemApiProps>;
  Header: React.FC<HeaderApiProps>;
  Trigger: React.FC<TriggerApiProps>;
  Panel: React.FC<PanelApiProps>;
};
```

### HeaderApiProps

```typescript
type HeaderApiProps<Value = any> = {
  /**
   * Allows you to replace the component's HTML element with a custom template.
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Header.State<Value>) => TemplateResult) | TemplateResult;
};
```

### ItemApiProps

```typescript
type ItemApiProps<Value = any> = {
  /** A unique value that identifies this accordion item. */
  value?: Value;
  /** Event handler called when the panel is opened or closed. */
  onOpenChange?: (open: boolean, eventDetails: Item.ChangeEventDetails) => void;
  /**
   * Whether the component should ignore user interaction.
   * @default false
   */
  disabled?: boolean;
  /**
   * Allows you to replace the component's HTML element with a custom template.
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Item.State<Value>) => TemplateResult) | TemplateResult;
};
```

### PanelApiProps

```typescript
type PanelApiProps<Value = any> = {
  /**
   * Allows the browser's built-in page search to find and expand the panel contents.
   * Overrides the `keepMounted` prop and uses `hidden="until-found"`
   * to hide the element without removing it from the DOM.
   * @default false
   */
  hiddenUntilFound?: boolean;
  /**
   * Whether to keep the element in the DOM while the panel is hidden.
   * This prop is ignored when `hiddenUntilFound` is used.
   * @default false
   */
  keepMounted?: boolean;
  /**
   * Allows you to replace the component's HTML element with a custom template.
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Panel.State<Value>) => TemplateResult) | TemplateResult;
};
```

### RootApiProps

```typescript
type RootApiProps<Value = any> = {
  /** The uncontrolled value of the item(s) that should be initially expanded. */
  defaultValue?: Value[];
  /** The controlled value of the item(s) that should be expanded. */
  value?: Value[];
  /** Event handler called when an accordion item is expanded or collapsed. */
  onValueChange?: (value: Value[], eventDetails: Root.ChangeEventDetails) => void;
  /**
   * Allows the browser's built-in page search to find and expand the panel contents.
   * Overrides the `keepMounted` prop and uses `hidden="until-found"`
   * to hide the element without removing it from the DOM.
   * @default false
   */
  hiddenUntilFound?: boolean;
  /**
   * Whether to loop keyboard focus back to the first item
   * when the end of the list is reached while using the arrow keys.
   * @default true
   */
  loopFocus?: boolean;
  /**
   * Whether multiple items can be open at the same time.
   * @default false
   */
  multiple?: boolean;
  /**
   * Whether the component should ignore user interaction.
   * @default false
   */
  disabled?: boolean;
  /**
   * The visual orientation of the accordion.
   * @default 'vertical'
   */
  orientation?: Orientation;
  /**
   * Whether to keep the element in the DOM while the panel is closed.
   * This prop is ignored when `hiddenUntilFound` is used.
   * @default false
   */
  keepMounted?: boolean;
  /**
   * Allows you to replace the component's HTML element with a custom template.
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Root.State<Value>) => TemplateResult) | TemplateResult;
};
```

### TransitionStatus

```typescript
type TransitionStatus = 'starting' | 'ending';
```

### TriggerApiProps

```typescript
type TriggerApiProps<Value = any> = {
  /**
   * Whether the component renders a native `<button>` element when replacing it
   * via the `render` prop.
   * Set to `false` if the rendered element is not a button.
   * @default true
   */
  nativeButton?: boolean;
  /**
   * Allows you to replace the component's HTML element with a custom template.
   * Accepts a `TemplateResult` or a function that returns the template to render.
   */
  render?: ((props: HTMLProps, state: Trigger.State<Value>) => TemplateResult) | TemplateResult;
};
```

## Canonical Types

Maps `Canonical`: `Alias` — Use Canonical when its namespace is already imported; otherwise use Alias.

- `Root.State`: `RootState`
- `Root.ChangeEventReason`: `RootChangeEventReason`
- `Root.ChangeEventDetails`: `RootChangeEventDetails`
- `Root.DataAttributes`: `RootDataAttributes`
- `Item.State`: `ItemState`
- `Item.ChangeEventReason`: `ItemChangeEventReason`
- `Item.ChangeEventDetails`: `ItemChangeEventDetails`
- `Item.DataAttributes`: `ItemDataAttributes`
- `Header.State`: `HeaderState`
- `Header.DataAttributes`: `HeaderDataAttributes`
- `Trigger.State`: `TriggerState`
- `Trigger.DataAttributes`: `TriggerDataAttributes`
- `Panel.State`: `PanelState`
- `Panel.DataAttributes`: `PanelDataAttributes`
- `Panel.CssVars`: `PanelCssVars`
