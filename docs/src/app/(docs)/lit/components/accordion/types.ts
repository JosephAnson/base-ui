import { createMultipleTypes } from 'docs/src/utils/createTypes';
import { Accordion } from './types-component';

export const { types: TypesAccordion } = createMultipleTypes(import.meta.url, Accordion);
