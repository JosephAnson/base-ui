import { createMultipleTypes } from 'docs/src/utils/createTypes';
import { RadioDocumentation } from './types-component';

export const { types: TypesRadio } = createMultipleTypes(import.meta.url, RadioDocumentation);
