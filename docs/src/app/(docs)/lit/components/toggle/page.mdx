# Toggle

<Subtitle>A two-state button that can be on or off.</Subtitle>
<Meta
  name="description"
  content="A high-quality, unstyled Lit toggle component that displays a two-state button that can be on or off."
/>

import { DemoToggleHero } from './demos/hero';
import { TypesToggle } from './types';

<DemoToggleHero />

## Anatomy

Import the helper and use it as a single part:

```ts title="Anatomy"
import { Toggle } from '@base-ui/lit/toggle';
import { html } from 'lit';

html`${Toggle({ children: 'Bold' })}`;
```

## API reference

<TypesToggle />

### Toggle Data Attributes

| Attribute      | Description                                |
| -------------- | ------------------------------------------ |
| `data-pressed` | Present when the toggle button is pressed. |

export const metadata = {
  keywords: [
    'Lit Toggle',
    'Toggle Button Component',
    'Two State Button',
    'Pressed State Button',
    'Accessible Toggle',
    'Base UI Lit',
  ],
};
